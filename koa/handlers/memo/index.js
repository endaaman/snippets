const getMemos = require('./get-memos.js')


function aggregateMemos(memos) {
  const tags = {}

  for (const memo in memos) {
  }

  return {
  }
}


module.exports = {
  getMemos,
  aggregateMemos,
}
